{{- $ing := .Values.ingress -}}
{{- if and $ing.enabled ($ing.control.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "sub2api.fullname" . }}-control
  labels:
    {{- include "sub2api.labels" . | nindent 4 }}
    app.kubernetes.io/component: control
  annotations:
    external-dns.alpha.kubernetes.io/hostname: >-
      {{ $ing.control.host }}{{ range $ing.control.extraHosts }},{{ .host }}{{ end }}
    external-dns.alpha.kubernetes.io/cloudflare-proxied: {{ $ing.cloudflareProxied | quote }}
    nginx.ingress.kubernetes.io/proxy-body-size: {{ $ing.maxBodySize | quote }}
    {{- with $ing.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if $ing.className }}
  ingressClassName: {{ $ing.className | quote }}
  {{- end }}
  {{- if $ing.control.tls.enabled }}
  tls:
    - hosts:
        - {{ $ing.control.host | quote }}
        {{- range $ing.control.extraHosts }}
        - {{ .host | quote }}
        {{- end }}
      secretName: {{ default (printf "%s-control-tls" (include "sub2api.fullname" .)) $ing.control.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ $ing.control.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "sub2api.fullname" . }}-control
                port:
                  name: http
    {{- range $ing.control.extraHosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "sub2api.fullname" $ }}-control
                port:
                  name: http
    {{- end }}
{{- end }}
